<Window x:Class="Test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:graphsharp="clr-namespace:GraphSharp.Controls;assembly=GraphSharp.Controls"
        xmlns:local="clr-namespace:Test"
        xmlns:zoom="clr-namespace:WPFExtensions.Controls;assembly=WPFExtensions"
        mc:Ignorable="d"
        x:Name="root"
        Title="MainWindow" Height="2000" Width="2000">
    <Grid>
        <zoom:ZoomControl Margin="344,0,0,0">
            <graphsharp:GraphLayout x:Name="graphLayout"
                                Graph="{Binding GraphToVisualize, ElementName=root}"
                                LayoutAlgorithmType="FR"
                                OverlapRemovalAlgorithmType="FSA"
                                HighlightAlgorithmType="Simple" Margin="-3,0"/>
        </zoom:ZoomControl>
        <ScrollViewer Margin="0,6,1654,0">
            <TextBlock x:Name="TxtOut" TextWrapping="Wrap"
                 FontSize="14"
                 ScrollViewer.VerticalScrollBarVisibility="Visible"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 Text="{Binding ElementName=TxtOut, Path=Text}"/>
        </ScrollViewer>
    </Grid>
</Window>
